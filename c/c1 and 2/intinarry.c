#include <stdio.h>
int main(){
  int array[3]; // creats space to hold integers.
  array[0] = 18;
  array[1] = 137;
  array[2] = 8;

  printf(" first element is %d.\n", array[0]);
  printf(" second element is %d.\n", array[1]);
  printf(" third element is %d.\n", array[2]);


  return 0;
}